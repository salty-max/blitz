import { Meta, Canvas } from '@storybook/addon-docs/blocks'

import * as PageHeadingStories from './page-heading.stories'

<Meta of={PageHeadingStories} />

# PageHeading

A page's primary heading, set in the Anton display face. One per view, above the
content.

<Canvas of={PageHeadingStories.Default} />
